
sources:
  - name: jaffle_shop
    database: raw  
    schema: jaffle_shop
    config:
      freshness:
        warn_after:
          count: 12
          period: hour
        error_after:
          count: 1
          period: day
      loaded_at_field: _ETL_LOADED_AT      
    tables:
      - name: orders
      - name: customers
        freshness: null

 #  - name: stripe
 #   database: raw
 #   schema: stripe
 #   tables:
 #     - name: payment